# Reusable UI boundary

This directory is reserved for small, product-agnostic interface primitives
such as buttons, containers, labels, and accessible controls. Components will
be introduced only when a section requires them.
