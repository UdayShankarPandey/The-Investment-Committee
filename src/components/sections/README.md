# Future homepage sections

This directory will contain the final page-level compositions: `Header`,
`Hero`, `CommitteePipeline`, `ProductShowcase`, `DevilsAdvocate`, and
`CallToActionFooter`.

`DesignSystemShowcase.tsx` is an internal Phase 2 preview of visual tokens and
reusable primitives. It is not a homepage section and should be replaced in
`PageShell` when the homepage composition begins.
